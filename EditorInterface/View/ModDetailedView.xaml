<UserControl x:Class="EditorInterface.View.ModDetailedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EditorInterface.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                <TextBlock Text="id: "/>
                <TextBlock Text="{Binding Id}"/>
            </StackPanel>
            <TextBlock Text="{Binding Identifier}" Margin="0,0,0,3" Foreground="Aqua" FontSize="20" />
            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" TextAlignment="Justify" Foreground="Aqua"/>
        </StackPanel>
    </Grid>
</UserControl>
