<UserControl x:Class="EditorInterface.View.FactionGroupValueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EditorInterface.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border  Style="{StaticResource ElementBorder}" VerticalAlignment="Stretch">
        <Grid Margin="7">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="id: "/>
            <TextBlock Text="{Binding Id}" Grid.Column="1"/>
            <TextBlock Text="Display name: " Grid.Row="1"/>
            <TextBlock Text="{Binding SSDisplayName}" Grid.Row="1" Grid.Column="1"/>
            <TextBlock Text="Display name with article: " Grid.Row="2"/>
            <TextBlock Text="{Binding SSDisplayNameWithArticle}" Grid.Row="2" Grid.Column="1"/>
            <TextBlock Text="Ship name prefix: " Grid.Row="3"/>
            <TextBlock Text="{Binding ShipNamePrefix}" Grid.Row="3" Grid.Column="1"/>
        </Grid>
    </Border>
</UserControl>
